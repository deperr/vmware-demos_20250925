---

- name: Test secret variable from vault
  hosts: dp_rhel9-01
  gather_facts: false

  vars_files:
    - secrets.yml

  tasks:

    - name: Print message from vault
      ansible.builtin.debug:
        msg: "The value is: {{ foo }}"